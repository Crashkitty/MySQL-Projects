CREATE TABLE experimental_cats (
  id INT(11) NOT NULL AUTO_INCREMENT,
  name VARCHAR(255) NOT NULL,
  breed VARCHAR(255) NOT NULL,
  age INT(11) NOT NULL,
  gender ENUM('male', 'female') NOT NULL,
  weight DECIMAL(5, 2) NOT NULL,
  PRIMARY KEY (id)
);
